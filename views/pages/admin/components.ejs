<!-- Dashboard -->
<div class="d-flex flex-column flex-lg-row h-lg-full bg-surface-secondary">
    <!-- Vertical Navbar -->
    <%- include('../../components/navbar', { account })%>
    <!-- Main content -->
    <div class="h-screen flex-grow-1 overflow-y-lg-auto">
        <!-- Main -->
        <main class="py-6 bg-surface-secondary">
            <div class="container-fluid">
                <!-- Blog Table -->
                <h2 class="mb-4">
                    Components Name: <strong><%= componentName %></strong>
                </h2>
                <%- include('../../modules/' + componentName) %>
            </div>
        </main>
    </div>
</div>